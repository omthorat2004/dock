# Base URL of the FastAPI backend, including the /api/v1 prefix.
# NEXT_PUBLIC_ is required: the browser's axios instance reads this directly.
# Use localhost (not 127.0.0.1) so the auth cookies stay same-site with the app.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
